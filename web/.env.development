# Vite serves the page from :5173 while the Go API answers on :8080, so the
# dev build needs an explicit base URL. The deployed build has none: it is
# served by the same origin as the API.
VITE_API_URL=http://localhost:8080
